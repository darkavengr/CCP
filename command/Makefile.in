ASM=@asm@
CC=@cc@
LD=@ld@

all:
	$(CC) -c -w -ffreestanding  -g *.c
	$(ASM) @asm_opts@ i386/*.asm
	$(CC) -T ld.script.command -o ../bin/@arch@/COMMAND.RUN -ffreestanding -nostdlib `find -iname \*.o` -lgcc -Wl,-Map -Wl,../command.map

